<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>metass-module-pay</artifactId>
        <groupId>top.metass.api</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>metass-module-pay-biz</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        pay 模块，我们放支付业务，提供业务的支付能力。
        例如说：商户、应用、支付、退款等等
    </description>

    <dependencies>
        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-module-pay-api</artifactId>
            <version>${revision}</version>
        </dependency>

        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-module-member-api</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- 业务组件 -->
        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-spring-boot-starter-biz-operatelog</artifactId>
        </dependency>
        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-spring-boot-starter-biz-pay</artifactId>
        </dependency>
        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-spring-boot-starter-biz-tenant</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-spring-boot-starter-redis</artifactId>
        </dependency>

        <!-- Job 定时任务相关 -->
        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-spring-boot-starter-job</artifactId>
        </dependency>

        <!-- 消息队列相关 -->
        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-spring-boot-starter-mq</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>top.metass.api</groupId>
            <artifactId>metass-spring-boot-starter-excel</artifactId>
        </dependency>

    </dependencies>

</project>
